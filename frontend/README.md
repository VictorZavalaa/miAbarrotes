# Frontend (cliente de tienda)

Esta carpeta será para la app web (React + Vite) que abrirás en Chrome desde la laptop del mostrador.

La laptop funcionará como **cliente**, consumiendo la API del backend corriendo en la Raspberry Pi dentro de la red local.
